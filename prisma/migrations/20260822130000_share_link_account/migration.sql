-- Менторская ссылка на конкретный счёт. NULL — ссылка на все счета сразу
-- (так вели себя все ссылки до этой колонки, поэтому старые записи менять
-- не нужно).
--
-- Без внешнего ключа: счёт могут удалить, и ссылка на удалённый счёт должна
-- показывать пустой список, а не падать и не превращаться молча в ссылку
-- «на всё». Отсюда же и отсутствие ON DELETE SET NULL.
ALTER TABLE "ShareLink" ADD COLUMN "accountId" TEXT;
