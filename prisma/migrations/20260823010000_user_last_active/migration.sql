-- Реальное присутствие пользователя, отдельно от lastSeenAt.
--
-- lastSeenAt обновляет ЛЮБОЙ запрос с валидной сессией, а открытая вкладка
-- дашборда сама по себе опрашивает сервер раз в 30-60 секунд (синк, поддержка,
-- уведомления). Из-за этого «онлайн» в админке означал «где-то открыта
-- вкладка», а не «человек за экраном».
--
-- lastActiveAt пишет только маячок присутствия: вкладка видима И было действие
-- пользователя за последние минуты (см. components/PresenceBeacon.tsx).
ALTER TABLE "User" ADD COLUMN "lastActiveAt" TIMESTAMP(3);
