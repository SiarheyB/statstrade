-- Главная считает, сколько инструментов сканируется за ночь: DISTINCT symbol
-- по дневным свечам. Существующий индекс начинается с symbol, поэтому фильтр
-- по interval шёл полным проходом таблицы — а это самая большая таблица
-- котировок. С этим индексом запрос читает только нужные строки.
CREATE INDEX "ObCandle_interval_symbol_idx" ON "ObCandle"("interval", "symbol");
