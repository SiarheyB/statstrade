-- Ярлык с ценой у горизонтальной линии/луча: показывать или нет.
--
-- По умолчанию true — существующие уровни сразу получают подпись цены
-- (именно за ней уровень обычно и рисуют), выключается в меню рисунка.
ALTER TABLE "UserDrawing" ADD COLUMN "showPrice" BOOLEAN NOT NULL DEFAULT true;
