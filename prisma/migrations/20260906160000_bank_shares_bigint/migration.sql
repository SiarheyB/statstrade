-- Число акций банка не помещается в 32-битное целое.
--
-- При капитале в триллион и балансовой цене около ста долларов акций нужно
-- десять миллиардов — INT4 (максимум 2 147 483 647) столько не хранит, и
-- создание банка падало с ошибкой. У настоящих банков порядок тот же:
-- миллиарды акций по двузначной-трёхзначной цене.
ALTER TABLE "GameBank" ALTER COLUMN "totalShares" TYPE DOUBLE PRECISION;
ALTER TABLE "GameBank" ALTER COLUMN "sharesSold" TYPE DOUBLE PRECISION;
