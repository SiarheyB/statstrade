-- Трекинг последней активности юзера для адаптивного бэкоффа авто-синка.
-- Nullable, без дефолта: у существующих юзеров останется NULL и трактуется как
-- "активный" (×1), чтобы после деплоя ничей синк не замедлился до первого захода.
ALTER TABLE "User" ADD COLUMN "lastSeenAt" TIMESTAMP(3);
