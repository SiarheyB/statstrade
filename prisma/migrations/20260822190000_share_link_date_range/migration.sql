-- Период менторской ссылки: конкретные даты, выбранные в календаре.
-- NULL с любой стороны — граница не задана (так вели себя все ссылки до этой
-- колонки, поэтому старые записи трогать не нужно).
--
-- periodTo хранится как НАЧАЛО СЛЕДУЮЩИХ суток: в календаре пользователь
-- выбирает день целиком, и сделка, закрытая в 23:30 последнего дня, должна
-- попадать в выборку.
ALTER TABLE "ShareLink" ADD COLUMN "periodFrom" TIMESTAMP(3);
ALTER TABLE "ShareLink" ADD COLUMN "periodTo" TIMESTAMP(3);
