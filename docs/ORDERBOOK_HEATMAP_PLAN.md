# План: Orderbook Heatmap (как ClusterBtc) — Вариант B

> Полноценная тепловая карта лимитных ордеров с историей, как на clusterbtc.com / Bookmap:
> X — время, Y — цена, яркость ячейки — объём лимитных заявок на уровне; «стены» гаснут,
> когда цену через них проливают. Плюс слайдеры фильтрации мелких заявок.

## Решения (зафиксированы)
- **Вариант B** — полноценный heatmap с историей (не синтетика, в отличие от текущего `liqmap`).
- Отдельный **collector-сервис вне Vercel** (Railway/Fly/VPS) — постоянный WebSocket.
- Хранилище — та же **Neon Postgres**, новые таблицы.
- Фронт — новый раздел в текущем Next.js, рендер на canvas (переиспользуем подход `liqmap`).
- **Старт:** Binance Futures, пара **BTCUSDT** (дефолт, можно сменить).
- **Шаг ценового бина:** **$25** для BTC (дефолт, можно сменить).
- Collector живёт в подпапке репозитория `collector/` (отдельный package.json, отдельный деплой).

## Архитектура
```
Биржи (WS depth) → Collector (локальный стакан + ресинк) → Neon Postgres (агрегаты) → Next.js /api/orderflow → Canvas heatmap (+ SSE live)
```

---

## Чеклист этапов

- [x] **Этап 0 — Прототип collector локально.** ✅ ВЫПОЛНЕН. 1 пара, корректное поддержание стакана
      (snapshot + diff-стрим, контроль sequence, ресинк при разрыве), лог снапшотов в консоль.
      Цель: убедиться, что стакан собирается корректно.
- [x] **Этап 1 — Хранение.** ✅ ВЫПОЛНЕН. Prisma-модель `ObSnapshot` + миграция; collector пишет
      агрегированные снапшоты (бин по цене) в Neon; retention (очистка старых).
- [x] **Этап 2 — API + статичный heatmap.** ✅ ВЫПОЛНЕН. `GET /api/orderflow` (диапазон → грид);
      раздел `dashboard/orderflow` с canvas-рендером исторического среза.
- [x] **Этап 3 — Фильтрация + оверлей.** ✅ Два слайдера (мин. размер / яркость), свечи + линия
      цены, кросхейр с тултипом (цена/ликвидность), выбор символа/биржи/диапазона, пункт меню.
- [x] **Этап 4 — Live-хвост.** ✅ Polling последних данных каждые 3с + переключатель LIVE.
- [x] **Этап 5 — Деплой collector.** ✅ Артефакты готовы (`Dockerfile`, `.dockerignore`, `README.md`,
      healthcheck `/health`, reconnection/backoff). Сам деплой на Railway/Fly — действие пользователя.
- [x] **Этап 6 — Масштаб.** ✅ Мультибиржа (Binance/Bybit/OKX) + мультисимвол в collector,
      агрегатный режим «all» (сумма средних по биржам) в API/фронте.
- [x] **Этап 7 — Дельта/CVD.** ✅ Сбор сделок (`@trade`) → `ObTrade`, панель дельта + кумулятивная
      дельта под heatmap. Footprint-кластеры на свечах — оставлены как будущая доработка (самая
      тяжёлая по отрисовке; пользователь правит отображение отдельно).

---

## Риски / на что закладываться
- **Целостность стакана** — главный источник багов: ресинк при разрыве sequence обязателен.
- **Объём БД** — без retention/rollup Neon распухнет; параметры считать заранее.
- **Стоимость** хостинга collector (постоянный процесс).
- Рейт-лимиты REST при частых ресинках; выравнивание времени между биржами при агрегации.
- ToS/лицензии бирж на публичные WS при расширении.

---

## Журнал выполнения
<!-- После каждого выполненного этапа добавлять запись: дата, что сделано, файлы. -->

### 2026-06-27 — Этап 0 выполнен
- Создан прототип collector: `collector/prototype.mjs` (без зависимостей, глобальный WebSocket Node 24).
- Реализовано корректное поддержание локального стакана Binance USDⓈ-M Futures:
  REST-снапшот `/fapi/v1/depth?limit=1000` + diff-стрим `@depth@500ms`, правила 3-6
  (дроп старых, первое событие по `U<=lastUpdateId<=u`, непрерывность по `pu==prevU`),
  авто-ресинк при разрыве sequence, reconnection с задержкой.
- Проверено запуском (`RUN_MS=20000`): после фикса логики первого события — 0 ложных
  ресинков, стабильный поток апдейтов, корректные best bid/ask и «стены» в ±2% от mid.
- Дефолты: BTCUSDT, Binance Futures, шаг бина $25 (для следующих этапов).

### 2026-06-27 — Этап 1 выполнен
- Prisma-модель `ObSnapshot` (`prisma/schema.prisma`) + миграция
  `prisma/migrations/20260627120000_orderbook_snapshot/migration.sql`
  (таблица + индекс `(symbol, exchange, t)`). Применена локально; на Neon
  применится автоматически при деплое (`prisma migrate deploy` в build-скрипте).
- Collector оформлен как сервис в `collector/`:
  - `orderbook.mjs` — общий модуль поддержания стакана (вынесен из прототипа);
  - `index.mjs` — агрегация уровней по ценовым бинам в пределах ±DEPTH_PCT от mid,
    фильтр шума по нотионалу, multi-row INSERT через `pg`, retention (DELETE старше N дней, раз в час);
  - `package.json` (dep: `pg`), `.env.example`.
- Параметры (ENV, дефолты): BIN_SIZE=$25, SNAPSHOT_MS=2000, DEPTH_PCT=0.02,
  NOISE_MIN_NOTIONAL=$50k, RETENTION_DAYS=7.
- Проверено против локального Postgres: записано 6 снапшотов / 335 строк / 68 бинов,
  корректно видны крупные стены (напр. bid 59200 ≈ 300 BTC). БД-данные оставлены для разработки Этапа 2.
- ⚠️ Объём: при текущих параметрах ~10-15M строк/неделю (~1 ГБ). Для Neon free тесновато —
  ок до переезда на домашний Postgres; при необходимости поднять NOISE_MIN_NOTIONAL / SNAPSHOT_MS.

### 2026-06-27 — Этап 2 выполнен
- `src/lib/orderflow.ts` — построение грида из `ObSnapshot` (колонки = снапшоты во времени,
  бины = ценовые уровни, интенсивность = средний bid+ask), формат совместим с рендером liqmap.
- `src/app/api/orderflow/route.ts` — `GET /api/orderflow?range=15m|1h|4h|24h` (auth, дефолт BTCUSDT/binance-futures).
- `src/app/dashboard/orderflow/page.tsx` — canvas-рендер (viridis, offscreen-буфер, оси цены/времени,
  линия текущей цены, шкала цвета), переключатель диапазона, кнопка обновления.
- Навигация: пункт «Карта ордеров» в группе «Сервис» (`DashboardNav.tsx`), i18n-ключи `of.*`, `nav.orderflow` (EN/RU).
- 🐛 Фикс: столбец `t` переведён на `@db.Timestamptz(3)` — collector писал наивные локальные
  таймстемпы (рассинхрон −3ч с UTC), из-за чего окно выборки промахивалось. Модель + миграция обновлены.
- Проверено: API строит грид (cols/bins/maxVal корректны), heatmap отрисован в браузере —
  чётко видны горизонтальные «стены» лимиток, линия цены, оси. Скриншот подтверждён.

### 2026-06-27 — Этап 3 (частично): стиль ClusterBtc + свечи
- По фидбеку: первая версия заливала весь фон палитрой viridis (стиль liqmap) — неверно.
- Переделан рендер под стиль ClusterBtc/Bookmap: тёмный фон, лимитные «стены» рисуются
  как светлые горизонтальные полосы (серый→белый), прозрачность = интенсивность объёма.
- Добавлены японские свечи поверх: `fetchOrderflowCandles` в `src/lib/orderflow.ts`
  (Binance futures klines, интервал под диапазон), API возвращает `candles` + `from/to`.
- Рендер переведён на реальную временную ось X=[from,to]; Y-домен объединяет стакан и свечи;
  линия текущей цены с бейджем; легенда.
- ⚠️ На локальной БД мало истории стакана (collector работал минуты) → стены видны только
  у правого края. На проде при работе collector 24/7 заполнят всю ширину (как на скрине ClusterBtc).
- Осталось в Этапе 3: слайдеры фильтрации мелких заявок (порог отображения/яркости), кросхейр с тултипом.

### 2026-06-27 — Этапы 3–7 выполнены
- **Этап 3:** слайдеры «Min size» (порог отображения, %) и «Brightness» (гамма яркости),
  кросхейр с тултипом (время/цена/ликвидность), селекторы символа и биржи. `buildOffscreen`
  учитывает порог/гамму.
- **Этап 4:** LIVE-поллинг каждые 3с (тихое обновление без спиннера) + кнопка-переключатель LIVE.
- **Этап 5:** `collector/Dockerfile`, `.dockerignore`, `README.md` (инструкция деплоя Railway),
  HTTP healthcheck `/health` в `index.mjs`. Reconnection/backoff уже были.
- **Этап 6:** адаптеры `collector/bybit.mjs` (depth 200 — лимит linear), `collector/okx.mjs`
  (books, нормализация контрактов→базовые единицы по ctVal). `index.mjs` создаёт книгу на
  каждую пару (биржа×символ), ENV `SYMBOLS`/`EXCHANGES`. В `orderflow.ts`: агрегация перешла на
  равномерные временные корзины [from,to]; режим `exchange=all` (сумма средних по биржам).
- **Этап 7:** `collector/trades.mjs` (Binance `@trade`; `@aggTrade` в этом окружении молчит),
  модель `ObTrade` + миграция `20260627130000_orderbook_trades`, `computeDelta` в `orderflow.ts`,
  панель дельта+CVD под heatmap. Footprint-кластеры на свечах НЕ делал (отложено).
- Проверено в браузере: heatmap+свечи+слайдеры+кросхейр+LIVE+панель Δ/CVD рендерятся без ошибок;
  API `exchange=all` отдаёт агрегат; 3 биржи пишут (0 ресинков); дельта считается (CVD корректен).
- Находки: Binance ~1000 уровней (полный ±2%), Bybit linear max 200 (узкая полоса), OKX books 400
  (размер в контрактах). `@aggTrade` недоступен в текущем окружении — используем `@trade`.

### 2026-06-27 — Доработки отображения
- ✅ **Профиль объёма слева** (как у ClusterBtc): горизонтальная гистограмма текущей
  ликвидности стакана — bid зелёный, ask красный, длина по объёму. `buildOrderflowHeatmap`
  отдаёт `profileBid/profileAsk/profileMax` (по последнему снапшоту каждой биржи); левая
  панель шириной 76px в `page.tsx`, панель Δ/CVD выровнена по X. Проверено в браузере.

- ✅ **Footprint-кластеры в свечах** (как «Кластеры» ClusterBtc): объём сделок по ценовым
  уровням внутри свечи, цвет по дельте уровня (buy зелёный / sell красный), яркость по объёму.
  - Collector: `trades.mjs` копит объём по ценовым бинам; новая таблица `ObFootprint`
    (миграция `20260627140000_orderbook_footprint`), запись в `index.mjs`, retention.
  - API: `computeFootprint` (группировка по свече+цене) в `orderflow.ts`, отдаётся в `/api/orderflow`.
  - Фронт: отрисовка кластеров поверх heatmap под свечами + тумблер «Кластеры». Проверено в браузере.

- ✅ **B/A панель** дисбаланса: доля bid во времени (полная глубина + ±1% от mid), фон зон
  bid/ask. `computeBA` в `orderflow.ts`, отдельный canvas под панелью дельты.
- ✅ **Лента крупных рыночных ордеров**: сбор крупных сделок (порог `BIG_NOTIONAL`) в
  `trades.mjs` → таблица `ObBigTrade` (миграция `20260627150000`), `computeBigTrades`, панель-список.
- ✅ **Zoom/pan**: колесо = зум цены, Shift+колесо = зум времени (вокруг курсора), drag = сдвиг,
  двойной клик = сброс. view-домен в `page.tsx`, панели дельты/BA следуют по X. Подсказка в легенде.

## Все запланированные доработки отображения выполнены (2026-06-27)
Раздел `/dashboard/orderflow` теперь включает: heatmap лимитных стен + свечи, профиль объёма
слева, footprint-кластеры, дельту/CVD, B/A дисбаланс, ленту крупных ордеров, слайдеры фильтрации,
кросхейр, мультибиржу/агрегат, LIVE-обновление, zoom/pan. Проверено в браузере (без ошибок).

### 2026-06-27 — Production-доработки (по приоритетам)
**Приоритет 1:** авто-шаг бина под символ (`DEFAULT_BIN` + `BIN_SIZE_<SYMBOL>`); collector в Docker
(`docker-compose.yml`, `restart: unless-stopped`, healthcheck, env `OB_*`) — запущен, 9 фидов,
постоянный локальный сбор; динамические селекторы (`/api/orderflow/meta`).
**Приоритет 2:** мультибиржевые сделки (`trades.mjs` под Binance/Bybit/OKX; дельта/footprint/крупные
ордера по всем биржам; API принимает exchange single/all; тег биржи в ленте); числовые метки в кластерах при зуме.
**Приоритет 4:** сохранение настроек в localStorage (+ фикс гидрации).
Проверено: collector пишет 4 таблицы по всем биржам, heatmap заполняется по ширине, «all» агрегирует,
настройки восстанавливаются, ошибок в консоли нет.

### 2026-06-27 — Сужение скоупа: только Binance (futures + spot), BTC/ETH
- По запросу: собираем только **Binance**, символы **BTCUSDT, ETHUSDT**, рынки **futures и spot**.
  Bybit/OKX отключены (убраны из дефолтных `EXCHANGES`; адаптеры оставлены в коде на будущее).
- `orderbook.mjs` теперь market-aware: `market:"spot"` использует `stream.binance.com@depth@100ms`
  + `/api/v3/depth?limit=5000` и правило непрерывности по `U` (U == предыдущий u+1); futures — как было (pu).
- `trades.mjs`: адаптер `binance-spot` (`stream.binance.com@trade`); свечи спота — с `/api/v3/klines`.
- Дефолты: `SYMBOLS=BTCUSDT,ETHUSDT`, `EXCHANGES=binance-futures,binance-spot` (docker-compose, фронт-fallback).
- Проверено: 4 фида (futures+spot × BTC/ETH), все synced, resync=1; спот ~5000 уровней; селекторы и
  спотовый график корректны.

### 2026-07-05 — Таймфреймы, лимит зума и пользовательский часовой пояс
- Таймфреймы карты ордеров расширены: `5m` и `12h` добавлены, `24h` переименован в `1d`
  (везде синхронно: `RANGES`/`VISIBLE_CANDLES` в `page.tsx`, `TF_MS`/`CANDLES_IN_WINDOW`
  в `route.ts`, `CANDLE_INTERVAL`/`CANDLE_MS` в `orderflow.ts`).
- Ось времени показывает дату при переходе на новые сутки (раньше «03:00» повторялось
  без даты и выглядело как случайные числа); время наведённой свечи теперь на самой
  оси (жёлтый бокс), а не в отдельной всплывающей подсказке.
- Реализована пользовательская **таймзона отображения** (Настройки → Общие →
  «Часовой пояс», `src/lib/timezone.ts`): фиксированные смещения UTC-12..UTC+14 + auto,
  применяется к оси времени/тултипам orderflow и liqmap, к группировке сделок по дням
  в календаре, к «сегодня/завтра» в economic calendar, и через `format.ts:fmtDate` —
  ко всем сделкам/аккаунтам/новостям разом.
- Карта ликвидаций (`liqmap`): добавлены пределы зума по Y (`MIN_ZOOM_SPAN_FRAC`,
  `MAX_Y_ZOOM_OUT_MULT`) — раньше зумировалось до бесконечности/сплющивалось до пустых
  чёрных полос; диапазон цены грида расширен под дистанцию ликвидации низких плечей
  (3x/5x, `MAX_LEV_DIST` в `lib/liqmap.ts`) — раньше карта была почти пустой дальше пары
  процентов от цены, в отличие от CoinGlass.
- Общая пагинация: `src/components/Pagination.tsx` (свой скроллящийся список страниц,
  открывается вверх) — Trades/News/admin UsersTable (50 юзеров/страница).

## Остаётся (по убыванию приоритета)
- Объём БД при 9 фидах растёт быстро — следить за диском/`OB_RETENTION_DAYS` при недельном сборе.
- Bybit WS макс. 200 уровней (узкая полоса); OKX `ctVal` — ручная карта (расширить через instruments API).
- Свечи всегда с Binance (ценовой референс).
- Автоскролл за ценой при LIVE с активным зумом.
- LIVE: polling 3с → SSE/WS; кэш `/api/orderflow`; проверить `@aggTrade` на проде.
- Тесты на агрегацию; тонкая косметика (палитра/размеры/сворачивание панелей).
- Деплой collector в облако (Railway/Fly) — когда понадобится прод вне локалки.
- Замена polling на SSE/WS при росте нагрузки.
- Расширение глубины Bybit/OKX, точные ctVal через instruments API.
